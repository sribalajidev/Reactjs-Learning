import React from "react";

// Define a functional component named Syntax
function Syntax() {
  // JSX to render
  return <h1>Hello from Functional Component!</h1>;
}

// Export the component so it can be imported elsewhere
export default Syntax;
